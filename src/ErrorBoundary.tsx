import {Component,type ErrorInfo,type ReactNode} from 'react';
export class ErrorBoundary extends Component<{children:ReactNode},{error:boolean}>{state={error:false};static getDerivedStateFromError(){return {error:true}}componentDidCatch(error:Error,info:ErrorInfo){console.error('[Lumenwild render recovery]',error,info.componentStack)}render(){return this.state.error?<main className="cloud-loading" role="alert"><div className="mark"><span>✦</span><b>Lumenwild</b></div><h1>The trail became tangled</h1><p>Your Firebase save is still safe. Reload Lumenwild to return to the last confirmed action.</p><button className="primary" onClick={()=>location.reload()}>Reload safely</button></main>:this.props.children}}
