import {readdir,readFile,stat} from 'node:fs/promises';import {fileURLToPath} from 'node:url';import {join} from 'node:path';
const root=fileURLToPath(new URL('../dist/',import.meta.url)),html=await readFile(join(root,'index.html'),'utf8');if(!html.includes('<div id="root"></div>'))throw new Error('Production shell is missing the React root.');const assetRoot=join(root,'assets'),assets=await readdir(assetRoot),js=assets.filter(name=>name.endsWith('.js'));if(!js.length)throw new Error('Production build contains no JavaScript assets.');for(const name of js){const bytes=(await stat(join(assetRoot,name))).size;if(bytes>600_000)throw new Error(`${name} exceeds the 600 KB JavaScript budget.`)}for(const required of ['journal-banner.png','quest-board-banner.png','workbench-market-banner.png'])if(!assets.includes(required))throw new Error(`Missing ${required}.`);console.log(`Verified production shell and ${js.length} JavaScript chunks.`);
